{
  "name": "@osprey/proto",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Osprey protocol registry and code generator (Rust + Swift).",
  "scripts": {
    "generate": "node generate.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "smol-toml": "^1.7.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
